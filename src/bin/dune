(executable
 (name Driver)
 (public_name driver)
 (libraries
   core
   core_unix
   cmdliner
   ocaml-func.func
   ocaml-func.istd)
 ; Enable for debugging:
 ; (modes byte)
 (preprocess
  (pps ppx_let)))

(env
 (dev
  (flags
   (:standard -warn-error -A)))
 (release
  (ocamlopt_flags :standard -ccopt -static)))
